name: laboratorio_3
description: "Laboratorio de cómputo 3"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  toastification:
  pie_chart: ^5.4.0
  postgres:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
